export function TokenBadge({ type }: { type: "verified" }) {
  return (
    <span className="text-[10px] px-1.5 py-0.5 rounded bg-primary/15 text-primary border border-primary/30">
      VERIFIED
    </span>
  );
}
